import React from 'react';
import styles from '../../style';

const ShopButton = () => {
	return (
		<form action=''>
			<button className={`${styles.buttonStyle}`}>Do koszyka</button>
		</form>
	);
};

export default ShopButton;
